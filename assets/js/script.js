// use #currentDay to display current day and date

var currentDate = Date();
$("#currentDay").html(currentDate);

// addeventlistener on save button to append text area value as HTML

// addeventlistener on save button to save text area value and location to localStorage

// depending on the time of day, use .addClass (classes in CSS) to color code

// get values and locations from localStorage
